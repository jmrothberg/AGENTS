# =============================================================================
# Obedient Beast - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: .env contains secrets and is gitignored - never commit it!
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Backend Selection
# -----------------------------------------------------------------------------
# Choose which LLM to use: "lfm" (local), "openai", or "claude"
LLM_BACKEND=claude

# -----------------------------------------------------------------------------
# API Keys (only needed for the backend you're using)
# -----------------------------------------------------------------------------
# Anthropic Claude API key (get from: https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# OpenAI API key (get from: https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-your-key-here

# -----------------------------------------------------------------------------
# Local LFM Server (only if using LLM_BACKEND=lfm)
# -----------------------------------------------------------------------------
# URL of your local LFM server running MLX models
# Beast tries localhost:8000 first, then falls back to this URL
LFM_URL=http://localhost:8000
LFM_URL_REMOTE=http://YOUR_SERVER_IP:8000

# -----------------------------------------------------------------------------
# WhatsApp Access Control
# -----------------------------------------------------------------------------
# Phone numbers allowed to use Beast (comma-separated, with country code)
# Leave empty = ONLY the WhatsApp account owner can use Beast
ALLOWED_NUMBERS=+12025551234

# Group IDs that Beast will respond in (comma-separated)
# Leave empty = respond in all groups (but still respects RESPOND_TO_OTHERS)
# Find group IDs in terminal logs: [GROUP][120363424416853198@g.us]
ALLOWED_GROUPS=

# Should Beast respond to OTHER people's messages in groups?
# false = Beast only responds to YOUR messages in groups
# true = Beast responds to anyone in ALLOWED_NUMBERS, even in groups
RESPOND_TO_OTHERS=false

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port for the Beast HTTP server (WhatsApp bridge connects to this)
BEAST_PORT=5001

# -----------------------------------------------------------------------------
# MCP (Model Context Protocol) - External Tool Servers
# -----------------------------------------------------------------------------
# Enable MCP tool servers (filesystem, memory, brave-search, etc.)
MCP_ENABLED=true

# Brave Search API key for web search tool (get from: https://brave.com/search/api/)
BRAVE_API_KEY=BSA_your-api-key-here
