# =============================================================================
# Obedient Beast - Configuration Example
# =============================================================================
# Copy this file to ../.env (parent directory) and fill in your values
# cp .env.example ../.env
# =============================================================================

# LLM Backend Selection: "lfm", "openai", or "claude"
LLM_BACKEND=claude

# Your LFM Server (when LLM_BACKEND=lfm)
# Replace with your server's IP address
LFM_URL=http://192.168.1.100:8000

# API Keys (when using cloud LLMs)
ANTHROPIC_API_KEY=sk-ant-your-key-here
OPENAI_API_KEY=sk-your-key-here

# WhatsApp Allowlist - Only these numbers can interact with Beast
# Format: comma-separated with country code, e.g., +12025551234,+442071234567
# Leave empty or commented out to allow everyone (not recommended!)
ALLOWED_NUMBERS=+12025551234

# =============================================================================
# MCP (Model Context Protocol) - Phase 2
# =============================================================================
# Enable MCP to use external tool servers (filesystem, git, memory, etc.)
# Set to "true" to enable, "false" to disable
# When enabled, Beast will load tools from config/mcp_servers.json
MCP_ENABLED=false
