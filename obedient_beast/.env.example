# This file is a stub. Canonical env template is the repo-root .env.example.

#   cp ../.env.example ../.env
#   # LLM_BACKEND=lfm  (local Qwen).  lfm = local model, not LiquidAI-only.

# Python loads repo-root .env first, then this directory, then cwd.
# Do not copy this file to obedient_beast/.env as your only config.
