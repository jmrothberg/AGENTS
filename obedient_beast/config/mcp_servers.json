{
  "servers": {
    "filesystem": {
      "enabled": true,
      "command": "npx -y @modelcontextprotocol/server-filesystem /Users/jonathanrothberg",
      "description": "Read/write/search files anywhere on your computer",
      "local": true
    },
    "git": {
      "enabled": false,
      "command": "npx -y @modelcontextprotocol/server-git --repository /Users/jonathanrothberg/AGENTS/AGENTS",
      "description": "Git operations: commit, diff, log, branch, status",
      "local": true,
      "note": "Disabled - package has startup issues. Beast can use shell tool for git commands."
    },
    "memory": {
      "enabled": true,
      "command": "npx -y @modelcontextprotocol/server-memory",
      "description": "Persistent knowledge graph - Beast remembers things forever",
      "local": true
    },
    "sequential-thinking": {
      "enabled": false,
      "command": "npx -y @modelcontextprotocol/server-sequential-thinking",
      "description": "Complex reasoning - breaks down hard problems step by step",
      "local": true,
      "note": "Enable when you need Beast to solve complex multi-step problems"
    },
    "brave-search": {
      "enabled": false,
      "command": "npx -y @modelcontextprotocol/server-brave-search",
      "description": "Web search via Brave Search API",
      "local": false,
      "env": {
        "BRAVE_API_KEY": "your-brave-api-key-here"
      },
      "note": "Requires Brave Search API key - data goes to Brave servers"
    }
  },
  "_comment": "Set 'enabled': true for servers you want to use. All are local except brave-search."
}
